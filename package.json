{
  "name": "elasticsearch-lite-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Frontend for elasticsearch-lite: search bar, instant search, filters and highlighted results.",
  "scripts": {
    "build": "node web/build.mjs",
    "watch": "node web/build.mjs --watch",
    "lint": "eslint web",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.32.0",
    "eslint": "^9.32.0",
    "globals": "^16.3.0",
    "prettier": "^3.6.2",
    "sass": "^1.89.2"
  },
  "engines": {
    "node": ">=20"
  }
}
